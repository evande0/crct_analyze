# Instructions

1) Clone the repository
2) Open the `config.py` file to specify the location of your project JSON files as well as save locations
3) Using a command line interface (CLI), run `run_pipeline.py`
4) To see available flags, call `run_pipeline.py --help`
